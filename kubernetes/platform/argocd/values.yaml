global:
  domain: argocd.example.invalid
server:
  service:
    type: ClusterIP
  ingress:
    enabled: false
configs:
  params:
    server.insecure: "false"
  cm:
    application.resourceTrackingMethod: annotation+label
    exec.enabled: "false"
    statusbadge.enabled: "false"
  rbac:
    policy.default: role:readonly
