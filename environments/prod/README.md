# Production environment placeholder

When a separate production subscription is available, create a production root here with:

- its own backend configuration and state key;
- a provider targeting the production subscription;
- production-specific capacity, availability, and security settings.

Do not point production and development at the same Terraform state key.
